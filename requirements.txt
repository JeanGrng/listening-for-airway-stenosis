# Core: enough to run make_tables.py and figures/make_fig2.py
numpy>=1.24
pandas>=2.0
matplotlib>=3.7

# Training and evaluation (src/run_mil.py, src/run_mil_subtasks.py)
scikit-learn>=1.3
torch>=2.0

# Embedding extraction (src/embeddings/), needs a GPU and the dataset
torchaudio>=2.0
transformers>=4.35
huggingface_hub>=0.19
safetensors>=0.4
timm>=0.9
librosa>=0.10
pyarrow>=14.0
scipy>=1.11

# YAMNet only. Heavy, and unnecessary unless you re-extract YAMNet embeddings:
#   pip install tensorflow tensorflow-hub
